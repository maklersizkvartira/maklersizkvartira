'use client';

/** The listings hub: e'lonlar, reports and Top requests as tabs. */
import { ListingsHub } from '@/features/hubs/ListingsHub';

export default function ListingsPage() {
  return <ListingsHub />;
}
